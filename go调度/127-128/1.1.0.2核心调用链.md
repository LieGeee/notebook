
### 📘 GORM 核心语法系统记忆指南

#### 1. 标准操作公式

在 Go 中，一个标准的数据库操作代码块通常长这样：

Go

```
// 1. 接收者   2. 方法名   3. 必须传 ctx   4. 业务参数
func (r *Repository) DoSomething(ctx context.Context, data *model.Data) error {
    // 5. 链式调用：实例 -> 绑定上下文 -> 过滤条件 -> 核心动作 -> 获取错误
    return r.db.WithContext(ctx).Where("id = ?", data.ID).Updates(data).Error
}
```

---

#### 2. 必须记忆的“核心动作” (Finisher Methods)

这些方法会真正触发 SQL 的生成和执行。
### 2. 执行阶段 (Finisher Methods)

这些方法是“工厂”的**最后一道工序**，调用它们才会真正生成 SQL 并发送给数据库。

|**动作**|**底层 SQL**|**核心行为**|
|---|---|---|
|**`.First(&m)`**|`SELECT ... LIMIT 1`|**必带主键排序**。没查到会抛出 `ErrRecordNotFound`。|
|**`.Take(&m)`**|`SELECT ... LIMIT 1`|不带默认排序，性能比 `First` 略快。|
|**`.Find(&slice)`**|`SELECT ...`|查零条或多条。**没查到不会报错**，只会返回空数组。|
|**`.Create(&m)`**|`INSERT INTO ...`|自动回填 `ID`、`CreatedAt` 到结构体。|
|**`.Updates(map)`**|`UPDATE ...`|使用 `map` 会更新零值字段；使用 `struct` **默认跳过零值**。|
|**`.Delete(&m)`**|`DELETE ...`|如果 Model 有 `DeletedAt` 字段，自动变为 **软删除**。|

---

#### 3. 必须记忆的“链式配置” (Chainable Methods)

这些方法用于构建 SQL 的细节（类似于 MP 的 `QueryWrapper`）。

- **`.WithContext(ctx)`**：**最重要的规范**。用于超时控制和链路追踪。如果用户取消请求，数据库操作会立即停止，避免资源浪费。
    
- **`.Where(query, args…)`**：过滤条件，如 `.Where("name = ?", "jack")`。
    
- **`.Limit(n)` / `.Offset(n)`**：分页必备。
    
- **`.Order("column DESC")`**：排序。
    
- **`.Table("name")`**：手动指定表名（通常 GORM 会自动识别模型对应的表名）。
    

---

#### 4. 三个“底层直觉” (非常重要)

在写代码时，脑子里要时刻紧绷这根弦：

- **指针意识 (`&`)**：
    
    - 只要是涉及**把数据库里的东西拿出来**（查询）或者**让数据库把生成的东西塞回来**（插入回填），参数必须传地址（指针 `&`）。
        
    - **原因**：传值是副本，函数运行完副本就丢了，你的原始变量拿不到任何更新。
        
- **错误处理 (`.Error`)**：
    
    - GORM 不会 `throw Exception`。
        
    - 所有的成功或失败，最后都要通过 `.Error` 来获取。如果 `err != nil`，说明操作失败了。
        
- **资源节约 (`Context`)**：
    
    - 养成习惯，第一步永远是 `.WithContext(ctx)`。
        

---

### 📝 巩固练习

如果你现在要写一个方法：**“根据用户 ID (`userID`)，查询该用户下所有的短链接列表”**。

你会怎么组合这些“积木”？
```go
func (r *shortLinkRepository) ListByUserID(ctx context.Context, userID uint32) ([]*model.ShortLink, error) {
    var links []*model.ShortLink
    
    // 请在此处脑补或写出你的链式调用代码
    err := r.db.WithContext(ctx).Where("user_id = ?", userID).Find(&links).Error
    
    return links, err
}
```

**需要我为你演示一下如何处理常见的“数据未找到”错误（First 方法找不到数据时）吗？**
### 📝 练习 1：基础查询与错误处理

**场景**：用户输入一个短链代码 `code`，你需要从数据库中找到这条记录。 **要求**：

1. 使用 `First` 方法。
    
2. 必须包含 `context.Context`。
    
3. **核心考点**：如果数据库里没有这个 `code`，你应该如何判断并返回一个“未找到”的业务逻辑（即不报错，但返回 `nil, nil`），而不是直接把 GORM 的内部错误抛给上层？
    
```go
// 补全代码
func (r *shortLinkRepository) GetByCode(ctx context.Context, code string) (*model.ShortLink, error) {
    var link model.ShortLink
    // Your Code Here:
}
```

---

### 📝 练习 2：分页与计数 (Count)

**场景**：后台管理系统需要显示某个租户（`tenantID`）下所有的短链接，且需要支持分页。 **要求**：

1. 返回当前页的数据切片 `[]*model.ShortLink`。
    
2. 返回该租户下的总记录数 `total`。
    
3. **核心考点**：注意 `Count` 和 `Find` 的调用顺序，以及如何复用查询条件以避免写两遍 `Where`。
    
```go
// 补全代码
func (r *shortLinkRepository) ListByTenant(ctx context.Context, tenantID int64, page, limit int) ([]*model.ShortLink, int64, error) {
    var links []*model.ShortLink
    var total int64
    // Your Code Here:
}
```

---

### 📝 练习 3：更新中的“零值陷阱”

**场景**：用户想要禁用（Disable）某个短链接。数据库字段是 `is_active` (bool类型)。 **要求**：

1. 将 `is_active` 字段更新为 `false`（即 Go 中的零值）。
    
2. **核心考点**：如果你直接传入一个 `model.ShortLink{IsActive: false}` 结构体给 `.Updates()`，GORM 会更新这个字段吗？如果不会，你应该用什么方式来确保 `false` 能被成功写入数据库？
    
```go
// 补全代码
func (r *shortLinkRepository) DisableLink(ctx context.Context, code string) error {
    // Your Code Here:
}
```

---

### 📝 练习 4：原子性事务 (Transaction)

**场景**：当一个短链接被点击时，你不仅要获取它的 `TargetURL`，还要给它的 `click_count`（点击量）字段加 1。 **要求**：

1. 这两个动作必须在同一个事务中完成。
    
2. 使用 `r.db.Transaction` 闭包写法。
    
3. **核心考点**：在事务闭包内部，你应该使用哪个变量来执行数据库操作？（是 `r.db` 还是闭包参数里的那个变量？）
    
```go
// 补全代码
func (r *shortLinkRepository) GetAndIncrementClick(ctx context.Context, code string) (*model.ShortLink, error) {
    var link model.ShortLink
    // Your Code Here:
}
```